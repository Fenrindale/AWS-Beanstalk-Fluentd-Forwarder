option_settings:
  aws:elasticbeanstalk:application:environment:
    SWAP_SIZE_GB: '2'                 # 스왑 2GB
    UDP514_STRICT_VERIFY: '0'         # 검증 실패 시 배포 실패로 만들지 않음(Immutable 대비)
    UDP514_VERIFY_MAX_SECS: '600'     # 타깃 헬스 대기 10분